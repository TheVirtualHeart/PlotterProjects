require.config({
	paths: {
		calculator: "calculator",
		form: "form",
		mediator: "mediator",
		plots: "plots",
		pointBuffer: "pointBuffer",
		utility: "utility",
	},
});

require(["form"], function(form) {
	// initialize the form
});